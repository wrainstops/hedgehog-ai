{
  "name": "hedgehog-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Hedgehog AI - 本地 LLM 桌面应用",
  "scripts": {
    "dev": "pnpm --filter hedgehog-desktop dev",
    "start": "pnpm --filter hedgehog-desktop start",
    "build": "pnpm --filter hedgehog-desktop build",
    "setup": "pnpm install"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.0.0"
}
